version: 2
 
models:
  - name: customers
    description: Cleaned up data about our customers.
    columns:
      - name: id
        description: Primary key of the model
        tests:
          - unique
          - not_null